"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/dataviews/src/utils/filter-sort-and-paginate.ts
var filter_sort_and_paginate_exports = {};
__export(filter_sort_and_paginate_exports, {
  default: () => filterSortAndPaginate
});
module.exports = __toCommonJS(filter_sort_and_paginate_exports);
var import_remove_accents = __toESM(require("remove-accents"));
var import_date_fns = require("date-fns");
var import_deprecated = __toESM(require("@wordpress/deprecated"));
var import_date = require("@wordpress/date");
var import_constants = require("../constants.cjs");
var import_field_types = __toESM(require("../field-types/index.cjs"));
function normalizeSearchInput(input = "") {
  return (0, import_remove_accents.default)(input.trim().toLowerCase());
}
var EMPTY_ARRAY = [];
function getRelativeDate(value, unit) {
  switch (unit) {
    case "days":
      return (0, import_date_fns.subDays)(/* @__PURE__ */ new Date(), value);
    case "weeks":
      return (0, import_date_fns.subWeeks)(/* @__PURE__ */ new Date(), value);
    case "months":
      return (0, import_date_fns.subMonths)(/* @__PURE__ */ new Date(), value);
    case "years":
      return (0, import_date_fns.subYears)(/* @__PURE__ */ new Date(), value);
    default:
      return /* @__PURE__ */ new Date();
  }
}
function filterSortAndPaginate(data, view, fields) {
  if (!data) {
    return {
      data: EMPTY_ARRAY,
      paginationInfo: { totalItems: 0, totalPages: 0 }
    };
  }
  const _fields = (0, import_field_types.default)(fields);
  let filteredData = [...data];
  if (view.search) {
    const normalizedSearch = normalizeSearchInput(view.search);
    filteredData = filteredData.filter((item) => {
      return _fields.filter((field) => field.enableGlobalSearch).some((field) => {
        const fieldValue = field.getValue({ item });
        const values = Array.isArray(fieldValue) ? fieldValue : [fieldValue];
        return values.some(
          (value) => normalizeSearchInput(String(value)).includes(
            normalizedSearch
          )
        );
      });
    });
  }
  if (view.filters && view.filters?.length > 0) {
    view.filters.forEach((filter) => {
      const field = _fields.find(
        (_field) => _field.id === filter.field
      );
      if (field) {
        if (filter.operator === import_constants.OPERATOR_IS_ANY && filter?.value?.length > 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            if (Array.isArray(fieldValue)) {
              return filter.value.some(
                (filterValue) => fieldValue.includes(filterValue)
              );
            } else if (typeof fieldValue === "string") {
              return filter.value.includes(fieldValue);
            }
            return false;
          });
        } else if (filter.operator === import_constants.OPERATOR_IS_NONE && filter?.value?.length > 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            if (Array.isArray(fieldValue)) {
              return !filter.value.some(
                (filterValue) => fieldValue.includes(filterValue)
              );
            } else if (typeof fieldValue === "string") {
              return !filter.value.includes(fieldValue);
            }
            return false;
          });
        } else if (filter.operator === import_constants.OPERATOR_IS_ALL && filter?.value?.length > 0) {
          filteredData = filteredData.filter((item) => {
            return filter.value.every((value) => {
              return field.getValue({ item })?.includes(value);
            });
          });
        } else if (filter.operator === import_constants.OPERATOR_IS_NOT_ALL && filter?.value?.length > 0) {
          (0, import_deprecated.default)("The 'isNotAll' filter operator", {
            since: "7.0",
            alternative: "'isNone'"
          });
          filteredData = filteredData.filter((item) => {
            return filter.value.every((value) => {
              return !field.getValue({ item })?.includes(value);
            });
          });
        } else if (filter.operator === import_constants.OPERATOR_IS) {
          filteredData = filteredData.filter((item) => {
            return filter.value === field.getValue({ item }) || filter.value === void 0;
          });
        } else if (filter.operator === import_constants.OPERATOR_IS_NOT) {
          filteredData = filteredData.filter((item) => {
            return filter.value !== field.getValue({ item });
          });
        } else if (filter.operator === import_constants.OPERATOR_ON && filter.value !== void 0) {
          const filterDate = (0, import_date.getDate)(filter.value);
          filteredData = filteredData.filter((item) => {
            const fieldDate = (0, import_date.getDate)(field.getValue({ item }));
            return filterDate.getTime() === fieldDate.getTime();
          });
        } else if (filter.operator === import_constants.OPERATOR_NOT_ON && filter.value !== void 0) {
          const filterDate = (0, import_date.getDate)(filter.value);
          filteredData = filteredData.filter((item) => {
            const fieldDate = (0, import_date.getDate)(field.getValue({ item }));
            return filterDate.getTime() !== fieldDate.getTime();
          });
        } else if (filter.operator === import_constants.OPERATOR_LESS_THAN && filter.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return fieldValue < filter.value;
          });
        } else if (filter.operator === import_constants.OPERATOR_GREATER_THAN && filter.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return fieldValue > filter.value;
          });
        } else if (filter.operator === import_constants.OPERATOR_LESS_THAN_OR_EQUAL && filter.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return fieldValue <= filter.value;
          });
        } else if (filter.operator === import_constants.OPERATOR_GREATER_THAN_OR_EQUAL && filter.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return fieldValue >= filter.value;
          });
        } else if (filter.operator === import_constants.OPERATOR_CONTAINS && filter?.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return typeof fieldValue === "string" && filter.value && fieldValue.toLowerCase().includes(
              String(filter.value).toLowerCase()
            );
          });
        } else if (filter.operator === import_constants.OPERATOR_NOT_CONTAINS && filter?.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return typeof fieldValue === "string" && filter.value && !fieldValue.toLowerCase().includes(
              String(filter.value).toLowerCase()
            );
          });
        } else if (filter.operator === import_constants.OPERATOR_STARTS_WITH && filter?.value !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            return typeof fieldValue === "string" && filter.value && fieldValue.toLowerCase().startsWith(
              String(filter.value).toLowerCase()
            );
          });
        } else if (filter.operator === import_constants.OPERATOR_BEFORE && filter.value !== void 0) {
          const filterValue = (0, import_date.getDate)(filter.value);
          filteredData = filteredData.filter((item) => {
            const fieldValue = (0, import_date.getDate)(
              field.getValue({ item })
            );
            return fieldValue < filterValue;
          });
        } else if (filter.operator === import_constants.OPERATOR_AFTER && filter.value !== void 0) {
          const filterValue = (0, import_date.getDate)(filter.value);
          filteredData = filteredData.filter((item) => {
            const fieldValue = (0, import_date.getDate)(
              field.getValue({ item })
            );
            return fieldValue > filterValue;
          });
        } else if (filter.operator === import_constants.OPERATOR_BEFORE_INC && filter.value !== void 0) {
          const filterValue = (0, import_date.getDate)(filter.value);
          filteredData = filteredData.filter((item) => {
            const fieldValue = (0, import_date.getDate)(
              field.getValue({ item })
            );
            return fieldValue <= filterValue;
          });
        } else if (filter.operator === import_constants.OPERATOR_AFTER_INC && filter.value !== void 0) {
          const filterValue = (0, import_date.getDate)(filter.value);
          filteredData = filteredData.filter((item) => {
            const fieldValue = (0, import_date.getDate)(
              field.getValue({ item })
            );
            return fieldValue >= filterValue;
          });
        } else if (filter.operator === import_constants.OPERATOR_BETWEEN && Array.isArray(filter.value) && filter.value.length === 2 && filter.value[0] !== void 0 && filter.value[1] !== void 0) {
          filteredData = filteredData.filter((item) => {
            const fieldValue = field.getValue({ item });
            if (typeof fieldValue === "number" || fieldValue instanceof Date || typeof fieldValue === "string") {
              return fieldValue >= filter.value[0] && fieldValue <= filter.value[1];
            }
            return false;
          });
        } else if (filter.operator === import_constants.OPERATOR_IN_THE_PAST && filter.value?.value !== void 0 && filter.value?.unit !== void 0) {
          const targetDate = getRelativeDate(
            filter.value.value,
            filter.value.unit
          );
          filteredData = filteredData.filter((item) => {
            const fieldValue = (0, import_date.getDate)(
              field.getValue({ item })
            );
            return fieldValue >= targetDate && fieldValue <= /* @__PURE__ */ new Date();
          });
        } else if (filter.operator === import_constants.OPERATOR_OVER && filter.value?.value !== void 0 && filter.value?.unit !== void 0) {
          const targetDate = getRelativeDate(
            filter.value.value,
            filter.value.unit
          );
          filteredData = filteredData.filter((item) => {
            const fieldValue = (0, import_date.getDate)(
              field.getValue({ item })
            );
            return fieldValue < targetDate;
          });
        }
      }
    });
  }
  const sortByField = view.sort?.field ? _fields.find((field) => {
    return field.enableSorting !== false && field.id === view.sort?.field;
  }) : null;
  const groupByField = view.groupBy?.field ? _fields.find((field) => {
    return field.enableSorting !== false && field.id === view.groupBy?.field;
  }) : null;
  if (sortByField || groupByField) {
    filteredData.sort((a, b) => {
      if (groupByField) {
        const groupCompare = groupByField.sort(
          a,
          b,
          view.groupBy?.direction ?? "asc"
        );
        if (groupCompare !== 0) {
          return groupCompare;
        }
      }
      if (sortByField) {
        return sortByField.sort(a, b, view.sort?.direction ?? "desc");
      }
      return 0;
    });
  }
  let totalItems = filteredData.length;
  let totalPages = 1;
  if (view.page !== void 0 && view.perPage !== void 0) {
    const start = (view.page - 1) * view.perPage;
    totalItems = filteredData?.length || 0;
    totalPages = Math.ceil(totalItems / view.perPage);
    filteredData = filteredData?.slice(start, start + view.perPage);
  }
  return {
    data: filteredData,
    paginationInfo: {
      totalItems,
      totalPages
    }
  };
}
//# sourceMappingURL=filter-sort-and-paginate.cjs.map
